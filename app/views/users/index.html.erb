index view
<br>
<br>
<% @users.each do |user| %>
  <%=link_to user.fname + " " + user.lname, user_path(user) %>
  <br>
  <%=link_to "X", user_path(user), method: :delete, confirm: "Are you sure you want to do that?" %>
  <br>
<% end %>